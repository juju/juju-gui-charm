description "GUIServer"
author "Canonical"

start on (filesystem and net-device-up IFACE=lo)
stop on runlevel [!2345]

exec runserver.py --guiroot="{{gui_root}}" --apiurl="{{api_url}}" \
    --apiversion="{{api_version}}" --servetests --sslpath="{{ssl_path}}"
